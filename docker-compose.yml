services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: truthguard
      POSTGRES_PASSWORD: truthguard
      POSTGRES_DB: truthguard
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
